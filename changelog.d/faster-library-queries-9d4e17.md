### Changed

- Home, library grids, genre filters, and season pages load faster from the local catalog, and repeated Jellyfin requests reuse open connections instead of reconnecting each time. The first launch after updating rebuilds the local catalog once.
