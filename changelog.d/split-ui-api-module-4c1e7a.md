### Changed

- Internal cleanup of the app window's connection to the native app, with no visible change: its types, request client, and address helpers are now separate, and every request reads errors the same way.
