### Fixed

- Returning to Discover reuses recently loaded movie, series, and search results and restores the scroll position directly, without loading each page again. Expired results no longer trigger repeated scrolling and loading to chase the old position, and offscreen discovery posters load lazily.
