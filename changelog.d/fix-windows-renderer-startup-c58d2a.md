### Fixed

- Fixed Windows startup crashes with the built-in player by including the renderer's required shader compiler. Windows packages now verify actual video rendering before a release draft is prepared.
