### Fixed

- Account settings, playback preferences, and pending account deletions saved by a newer MediaFlick version are no longer treated as damaged. MediaFlick now refuses to open them and leaves the files unchanged, instead of setting them aside and falling back to an older backup or defaults.
